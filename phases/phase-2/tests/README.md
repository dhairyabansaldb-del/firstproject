# Phase 2 Tests

Planned test coverage:
- ingestion normalization logic (`scripts/ingest_zomato.py`)
- duplicate handling and quality report counts
- catalog filters (`city`, `cuisine`, `min_rating`, `max_cost_for_two`)
- API responses for missing catalog/report files
